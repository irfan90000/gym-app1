export default {
  setAge(state, addage) {
    state.health.age = addage;
  },
  setHeight(state, setheight) {
    state.health.height = setheight;
  },
  setWeight(state, addweight) {
    state.health.weight = addweight;
  },
  setActivity(state, addactivity) {
    state.health.activity = addactivity;
  },
  seBestActivity(state, addbestActivity) {
    state.health.bestActivity = addbestActivity;
  },
    
  };